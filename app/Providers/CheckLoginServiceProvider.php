<?php

namespace App\Providers;

use Illuminate\Support\ServiceProvider;

class CheckLoginServiceProvider extends ServiceProvider
{
    /**
     * Bootstrap any application services.
     *
     * @return void
     */
    public function boot()
    {
        // Đăng ký các sự kiện, middleware, hoặc các tác vụ khởi động khác.
    }

    /**
     * Register any application services.
     *
     * @return void
     */
    public function register()
    {
        // Đăng ký các service bindings hoặc các tác vụ cấu hình khác.
    }
}
